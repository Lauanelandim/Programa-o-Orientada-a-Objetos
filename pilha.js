/**
 * Pilha (LIFO - Last In, First Out): Último elemento a entrar é o
primeiro a sair.
 */

let pilha = [];
pilha.push(1); //Adiciona 1 à pilha
pilha.push(2); //Adiciona 2 à pilha
console.log(pilha.pop()); //Remove e retorna 2 (ultimo elemento)